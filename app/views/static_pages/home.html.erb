<% if signed_in? %>
  
  <div class="row">
    <aside class="span4">
      <section>
        <%= render 'shared/user_info' %>
      </section>
  </div>

      <section>
      <h2>Salesforce Objects</h2>
      
      <div class="options">
        <a href="<%= accounts_path %>" class="btn">Accounts</a>
        <a href="<%= opportunities_path %>" class="btn">Opportunities</a>
        <a href="<%= tasks_path %>" class="btn">Tasks</a>
        <a href="<%= marketing_strategy__cs_path %>" class="btn">Marketing Strategies</a>
      </div>
      
    </section>
<% else %>
  <div class="center hero-unit">
    <h1>R66T Salesforce App</h1>

    <h2>
      In order to use this App, you must Register.
    </h2>

    <%= link_to "Register now!", signup_path, 
                                class: "btn btn-large btn-primary" %>
  </div>

  <% link_to image_tag("rails.png", alt: "Rails"), 'http://rubyonrails.org/' %>
<% end %> 